# -*- coding: utf-8 -*-
# Titan Operations Sentinel - LangGraph multi-agent stack
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
# --- model providers: swap any time via TOS_MODEL or by dropping a different key in .env ---
langchain-google-genai>=2.0.0    # Gemini (free tier)
langchain-groq>=0.2.0            # Groq / Llama (free tier, fast)
langchain-openai>=0.2.0          # OpenAI  AND  OpenRouter (one key → many free models)
langchain-anthropic>=0.2.0       # Claude
langchain-mistralai>=0.2.0       # Mistral (free tier)
langchain-ollama>=0.2.0          # local / offline (no key)
python-dotenv>=1.0.0
